# Iris — Python dependencies
# Install: pip install -r requirements.txt

# Core
pystray>=0.19.0
Pillow>=10.0.0
pyserial>=3.5
requests>=2.31.0
psutil>=5.9.0

# Windows audio (mic mute, endpoint switching)
comtypes>=1.1.14
pycaw>=20181226

# Windows SMTC and Native Vision OCR (Windows.Media.Ocr)
winrt-runtime>=2.0.0
winrt-Windows.Media.Ocr>=3.0.0
winrt-Windows.Graphics.Imaging>=3.0.0

# Build / packaging
pyinstaller>=6.0

# HTML settings panel
pywebview>=4.0

# LAN access QR code
qrcode>=7.4

# HTTPS panel (self-signed cert generation for Screen Wake Lock on phones)
cryptography>=42.0

# Panel pairing password (Argon2id hashing)
argon2-cffi>=23.1.0
